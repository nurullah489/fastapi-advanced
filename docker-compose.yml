services:

  # ─── PostgreSQL ───────────────────────────────────────
  db:
    image: postgres:16-alpine
    container_name: fastapi_advanced_db
    env_file:
      - .env.docker
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"   # 5433 on host to avoid conflict with local PostgreSQL
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ─── FastAPI App ──────────────────────────────────────
  api:
    build: .
    container_name: fastapi_advanced_api
    env_file:
      - .env.docker          # separate env file for docker
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy   # waits for DB to be ready
    volumes:
      - .:/app               # mounts code for development
    restart: unless-stopped

volumes:
  postgres_data: